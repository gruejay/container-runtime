# Step 5: Filesystems (Part 3)

This will hopefully be the last installment of filesystems (for now- we will eventually need to
implement user-specific mounts, which we aren't delaing with yet). What we have left is:

- special `/dev/` filesystems and devices:
    - `/dev/pts`
    - `/dev/shm`
    - `/dev/null`
    - `/dev/random`
    - `/dev/urandom`

